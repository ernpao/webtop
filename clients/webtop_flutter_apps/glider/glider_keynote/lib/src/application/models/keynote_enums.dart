enum MouseButton {
  left,
  middle,
  right,
}

enum KeyboardModifier {
  alt,
  control,
  shift,
}

enum KeynoteCommandTopic {
  mouse,
  keyboard,
  broadcast,
}

enum KeynoteMouseCommandType {
  click,
  move,
  offset,
}
