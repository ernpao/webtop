import 'package:flutter/widgets.dart';

class ContinousControlSlider extends StatelessWidget {
  const ContinousControlSlider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
